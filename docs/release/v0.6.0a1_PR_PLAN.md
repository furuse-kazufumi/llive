# v0.6.0a1 PR Plan — Evolutionary stack v0.B/C/D/E 大規模前倒し

> Status: **draft (no-push 中)**. branch `optimize/core-2026-05-20` を 5 PR
> に論理分割するための内部メモ. push 解禁時に各 PR を順序通り送る.
>
> 作成: 2026-05-21 marathon 後半 (E.17/E.4/E.12 着地後).

## 0. 全体方針

- **base**: `main`
- **head**: `optimize/core-2026-05-20`
- 13 wave + 303 件の test 追加. **1 PR で出すと巨大** すぎる ので 5 つに
  論理分割し、レビュー対象を絞る.
- PR 間に依存関係があるため **順序通りマージ**する想定 (PR1 → 5).
- 各 PR は対応セット (実装 + tests + CHANGELOG section + README 行) を含む.
- 既存テストへの回帰なし. evolutionary 関連の合計 PASS は
  E.17/E.4/E.12 で +95, ruff 警告 0 を維持.

## 1. PR 1 — v0.B core (Genome / Population / EvolutionLoop)

**範囲**:

- `src/llive/perf/evolutionary/genome.py` (`Genome` / `GenomeBounds`)
- `src/llive/perf/evolutionary/individual.py` (`Individual` / `FitnessReport`)
- `src/llive/perf/evolutionary/population.py` (`Population` / `PopulationStats`)
- `src/llive/perf/evolutionary/crossover.py` (`UniformCrossover` /
  `BlendCrossover` / `SegmentCrossover`)
- `src/llive/perf/evolutionary/mutation.py` (`GaussianMutation` /
  `ResetMutation` / `ChainedMutation`)
- `src/llive/perf/evolutionary/selection.py` (Tournament / Roulette / Elitism)
- `src/llive/perf/evolutionary/loop.py` (`EvolutionLoop` /
  `EvolutionConfig` / `EvolutionResult`)
- `src/llive/perf/evolutionary/scheduler.py` (Serial / MP / Asyncio /
  `AsyncFitness`)
- `src/llive/perf/evolutionary/fitness.py` (`Fitness` / sphere / rosenbrock)
- `src/llive/perf/evolutionary/fitness_ucb.py` (UCB1 fitness wrapper)
- `src/llive/perf/evolutionary/lineage.py` (Winners + Mermaid)

**Title**: `feat(evolutionary): v0.B core — Genome/Population/EvolutionLoop +
parallel schedulers`

**Description**:
GA 古典 (Genome+Population+Selection+Crossover+Mutation) を skeleton + 並列
scheduler 3 種で実装. 派生 1 個体評価をプロセス分離 (subprocess) 経路と直結
する次 PR の前提.

## 2. PR 2 — v0.C subprocess transport + v0.D self-adaptive + meta mutation

**範囲**:

- `src/llive/perf/evolutionary/subprocess_scheduler.py` —
  `VariantSubprocessScheduler` (subprocess.run + ThreadPool + timeout +
  retries + cleanup).
- `src/llive/perf/evolutionary/self_adaptive.py` (Schwefel σSA-ES 38 dim).
- `src/llive/perf/evolutionary/meta_mutation.py` (`MetaMutation` /
  `strategy_distribution` / `pack_meta_strategy_bounds`).
- `src/llive/perf/evolutionary/llive_variant.py` +
  `src/llive/perf/evolutionary/llive_variant_extras.py` (LV 19→38/20/39 dim).
- `src/llive/variant_runner.py`.

**Title**: `feat(evolutionary): v0.C/D — VariantSubprocessScheduler +
SelfAdaptive + MetaMutation`

## 3. PR 3 — v0.E E.1-3 peer evaluation + EvaluationStyleGenome

**範囲**:

- `src/llive/perf/evolutionary/peer_evaluation.py` —
  `PeerEvaluationMatrix` (N×N, 共謀検出 3 指標, Mermaid) +
  `PeerFitnessAdapter`.
- v0.D SR-02 (`evaluation_style` dim) integration.
- `src/llive/perf/evolutionary/fitness_llm.py` (LLM-as-judge fitness, mock).

**Title**: `feat(evolutionary): v0.E E.1-3 — PeerEvaluationMatrix +
EvaluationStyleGenome`

## 4. PR 4 — v0.E E.10-18 diversity preservation (persona ontology + LHS + novelty + map-elites)

**範囲**:

- `src/llive/perf/evolutionary/persona.py` (PERSONA_ONTOLOGY 10 名 +
  PersonaComposition + PersonaCompositionMutation + persona_dissimilarity).
- `src/llive/perf/evolutionary/diversity.py` (LHS init / NoveltyScorer /
  DiversityPreservingBreedFilter / DiversityMonitor).
- `src/llive/perf/evolutionary/quality_diversity.py` (E.17 — CE-25 /
  CE-26: PersonaOverlapPenalty + MAPElitesGrid).
- `src/llive/perf/evolutionary/persona_import.py` (E.12 — CE-20:
  PersonaImportAlgorithm + PersonaImportPlan + PersonaZoneShareEvent).
- `src/llive/perf/evolutionary/mating.py` (MutualScorePairSelector +
  LexicaseSelection).
- `src/llive/perf/evolutionary/nsga2.py` (non_dominated_sort +
  crowding_distance + NSGA2Selection).
- `src/llive/perf/evolutionary/speciation.py` (NEAT 流種分け).
- `src/llive/perf/evolutionary/island_model.py` (3 topology + migration
  policy).
- `src/llive/perf/evolutionary/expert_council.py` +
  `src/llive/perf/evolutionary/expert_evolution.py`.

**Title**: `feat(evolutionary): v0.E E.10-18 — diversity preservation,
persona ontology, MAP-Elites, persona import`

## 5. PR 5 — v0.E E.4 governance skeleton + release wiring

**範囲**:

- `src/llive/perf/evolutionary/coevolution_governance.py` (E.4 —
  CollusionDetector / CoevolutionGovernance / GovernanceReport /
  collusion_risk_score) + 連携:
    - `llive.approval.bus.ApprovalBus`
    - `llive.cognitive_mesh.tonic_risk.TonicRiskMonitor`
- `CHANGELOG.md` v0.6.0a1 entry (E.17 / E.12 / E.4 sections).
- `README.md` v0.6.0a1 ステータス行.
- `docs/release/v0.6.0a1_PR_PLAN.md` (この文書).
- `docs/requirements_v0.E_competitive_coevolution.md`.

**Title**: `feat(evolutionary)+docs: v0.E E.4 governance skeleton + v0.6.0a1
release wiring`

## 6. リリース手順 (push 解禁後)

1. PR 1 を `main` にマージ → CI green 確認.
2. PR 2 を `optimize/core-2026-05-20` から rebase → マージ.
3. PR 3-5 同手順.
4. `git tag v0.6.0a1 <merge-sha>` + GitHub Release notes (CHANGELOG 引用).
5. `python -m build && twine upload --repository testpypi dist/*` で
   alpha 配布.
6. PyPI 本配布は v0.6.0 (stable) で行う. v0.6.0a1 は test pypi のみ.

## 7. リスクとロールバック

- **依存順序破綻**: PR 3 (peer_evaluation) が無いと PR 4 / 5 が compile
  しないため必ず順序遵守.
- **巨大 diff**: PR 4 が最大 (diversity / persona / mating / NSGA-II /
  speciation / island_model + 11 files). レビュアー負荷高い → 必要なら
  PR 4 を 4a (persona) / 4b (diversity + QD) / 4c (NSGA-II + speciation +
  island) に再分割可.
- **ruff 警告**: ruff check `src/llive/perf/evolutionary` が 0 警告で
  あること.
- **rollback**: 各 PR は機能単位なので revert で原状復帰可能. ただし
  PR 1-3 を revert すると PR 4-5 が壊れる依存.

---

> このファイルは marathon の作業ログを兼ねる. 各 PR を実際に分割して送る
> ときに参照される. 後続セッションで内容を更新して良い.
